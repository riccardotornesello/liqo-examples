# liqo-examples

- ansible-kubeadm: prepare some VMs to use kubeadm
- registry-proxy: docker compose for container image cache
- test-network: python script to print the ping matrix
- testbench: bash script to create a test environment
